export const registryText = (ctx) => {
    return `Add account <b>details, avatar, packages and socials</b>.

After entering some details save them if you are 
<b>not willing to apply for review directly</b>,
So you can apply when you feel ready

One should enter at least 1 social account and 1 promotion package`;
  };
export const accountText = (ctx, status) => {
    return `My account - status: ${status}`;
  };